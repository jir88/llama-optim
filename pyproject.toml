[project]
name = "llama-optim"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "optuna>=4.9.0",
    "pandas>=3.0.5",
    "requests>=2.34.2",
]
